using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Mirror;

public class ObjectClick : NetworkBehaviour
{
    /*[SerializeField] private Transform[] players = new Transform[2];
    [SerializeField] private LineController line;
    private TargetClick target;
    private void Start()
    {
        
        line.SetUpLine(players);
        if (isLocalPlayer)
        {
            players[0] = this.gameObject.transform;
        }
    }
    private void Update()
    {
        line.SetUpLine(players);
    }
    private void Click()
    {
        line.enabled = true;
        if(target.isClick = true)
        {
            players[1] = target.objecthit;
        }
    }*/
}
